# Events API Endpoint
# This endpoint should return a JSON response with Luma events schema
# See docs/luma_events.json for the expected format
EVENTS_ENDPOINT=http://localhost:1337/luma_events.json

# Optional: API timeout in milliseconds
EVENTS_TIMEOUT=5000
